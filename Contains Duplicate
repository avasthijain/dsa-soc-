class Solution {
  public:    
    int getSingle(vector<int>& arr) {
        int xr = 0;
        int n = arr.size();
        for (int i = 0; i < n; i++) {      
            xr = xr ^ arr[i];
        }

        return xr;
    }
};
